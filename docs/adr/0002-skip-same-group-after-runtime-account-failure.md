---
status: accepted
---

# 动态路由账号失败后直接跨到下一分组

低价优先、响应优先和自定义顺序路由中，一个分组所选账号在真实请求里发生可重试上游失败后，本次请求不继续尝试该分组的其他账号，而是直接进入下一个有序候选分组；失败账号交由独立探测验证，验证仍失败才进入 30 秒、60 秒、120 秒和 300 秒恢复节奏。该决策用探测流量承担账号排障，避免用户请求连续撞到同一分组内尚未发现的失败账号；固定单分组路由仍保留原有组内处理。
