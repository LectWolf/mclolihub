---
status: accepted
---

# 分离分组健康观测与 API 密钥路由策略

分组探测负责产生健康观测，不统一接管所有 API 密钥路由：固定分组密钥保留用户的明确选择并绕过分组健康门控，动态多分组路由才使用健康观测过滤候选；所有模式仍受用户设置的最大倍率限制。响应优先以最近 6 小时、合并分组全部模型的真实用户 TTFT 为主要依据，只有所有候选都没有真实样本时才回退到探测 TTFT；这是因为主动探测与真实请求的负载、输出和网络路径不同，直接混排会系统性高估探测性能并产生错误路由。上游账号余额不足作为独立账号运行状态，由管理员手动恢复，不升级为分组级永久状态。
