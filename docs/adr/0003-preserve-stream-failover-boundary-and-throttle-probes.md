---
status: accepted
---

# 保留流式故障转移边界并限制即时探测

分组路由不改变现有流式语义：在响应写出首个语义内容前允许故障转移，写出后按原协议结束当前流，不拼接第二个上游响应。真实请求发现账号失败时允许异步触发一次账号验证探测，但同一账号 10 分钟内最多触发一次即时验证；验证失败再进入既定恢复节奏，以避免“可用/不可用”抖动造成探测风暴。分组正常探测间隔可配置为 30 秒到 1 小时，默认 10 分钟。
