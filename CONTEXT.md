# Sub2API Domain Context

Sub2API 将用户 API 密钥请求路由到分组内的上游账号，并负责健康判断、计费和故障恢复。本词汇表统一分组健康与多分组路由相关语言。

## Language

**分组（Group）**:
一组可承接同类用户请求的上游账号，也是倍率、模型能力和路由选择的业务对象。
_Avoid_: 渠道、线路

**分组探测（Group Probe）**:
系统代表分组依次检查账号可用性的主动请求；一次探测轮次在首个账号成功后结束。
_Avoid_: 用户测速、渠道监控

**探测轮次（Probe Sweep）**:
按分组内账号优先顺序执行的一轮探测；任意账号成功则轮次成功，全部账号失败则轮次失败。
_Avoid_: 批量检测、全量成功检查

**分组健康（Group Health）**:
由最近探测结果和可恢复故障共同形成的分组运行状态，用于多分组路由的候选过滤。
_Avoid_: 账号状态、渠道状态

**账号余额不足（Account Balance Exhausted）**:
上游明确表示账号额度或余额无法继续承担请求的独立运行状态；它属于上游账号，不是用户钱包余额不足，需由管理员手动恢复。
_Avoid_: 用户余额不足、普通账号错误

**真实用户性能（Observed User Performance）**:
由一个分组全部模型的真实用户请求共同产生的首字延迟等性能数据；它反映分组整体用户体验，不与主动探测性能混为同一指标。
_Avoid_: 探测速度、理论延迟

**固定分组路由（Fixed Group Routing）**:
API 密钥始终使用指定单个分组的路由方式；分组探测只用于展示，不参与该方式的候选过滤。
_Avoid_: 强制健康路由

**低价优先路由（Cheapest-first Routing）**:
在可用候选分组中，按用户实际生效倍率从低到高选择分组的路由方式。
_Avoid_: 成本估算路由、账号成本优先

**响应优先路由（Fastest-first Routing）**:
在可用候选分组中，优先按真实用户首字延迟选择分组；仅当所有候选都没有真实用户样本时才使用探测首字延迟。
_Avoid_: 探测优先路由、最低网络延迟路由

**自定义顺序路由（Custom-order Routing）**:
用户为 API 密钥选定多个分组并明确排列优先级的路由方式。
_Avoid_: 随机路由、管理员排序

**跨分组故障转移（Cross-group Failover）**:
动态路由中的账号在真实请求中失败后，本次请求跳过该账号所在分组的其余账号，直接尝试下一个候选分组。
_Avoid_: 同组账号重试、分组内故障转移

**即时验证探测（Immediate Verification Probe）**:
真实请求发现账号失败后触发的单次快速复核；同一账号在一个限频窗口内最多触发一次，复核失败才进入恢复节奏。
_Avoid_: 用户请求重放、即时恢复

**最大倍率（Maximum Rate Multiplier）**:
用户为单个 API 密钥设置的倍率上限；实际生效倍率超过该上限的分组不能承接该密钥的请求。
_Avoid_: 价格上限、账号倍率上限

**禁用分组（Excluded Group）**:
用户在低价优先或响应优先路由中明确排除的分组；它仍属于用户可访问范围，但不参与该 API 密钥的候选选择。
_Avoid_: 停用分组、管理员禁用

**恢复节奏（Recovery Schedule）**:
失败账号按 30 秒、60 秒、120 秒和 300 秒间隔接受重新探测的恢复过程。
_Avoid_: 固定轮询、立即重试
